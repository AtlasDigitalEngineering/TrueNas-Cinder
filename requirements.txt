# Cinder is the runtime platform: this driver is loaded *by* a Cinder
# deployment, which already provides it.
#
# It is NOT installed for the api_client unit tests (tests/unit), which run
# on `requests` alone so the common CI job stays fast. It IS installed for
# the driver tests (tests/driver), which import truenas_cinder_driver.driver
# — see driver-test-requirements.txt.
#
# The proper dependency split between this file, test-requirements.txt,
# driver-test-requirements.txt and pyproject.toml is issue #23.
cinder>=26.0.0,<27.0.0
requests>=2.31.0
