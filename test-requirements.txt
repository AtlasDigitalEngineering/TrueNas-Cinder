pytest>=7.4.0
pytest-cov>=4.1.0
coverage>=7.2.0
flake8>=6.0.0
tox>=4.4.0

# Runtime dependency of the package under test. Declared here so unit tests
# can run without installing Cinder — see the note in requirements.txt.
requests>=2.31.0
