# Dependencies for the driver unit tests (tests/driver), which import
# truenas_cinder_driver.driver and therefore need Cinder itself.
#
# Kept separate from test-requirements.txt on purpose: Cinder pulls ~58
# direct dependencies (the whole oslo stack, SQLAlchemy, eventlet,
# taskflow, os-brick), and the api_client tests in tests/unit need none of
# them. Splitting keeps the common CI job fast while still testing the
# driver against the real base classes rather than a stub.
#
# Pinned to the 2025.1 series, matching the Kolla-Ansible deployment target.
-r test-requirements.txt
cinder>=26.0.0,<27.0.0
